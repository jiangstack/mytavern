# 小镇模块详细设计

> 定位：生活模拟玩法（类《模拟人生》），LLM 驱动小镇 NPC 的日常与相遇，玩家可上帝视角旁观，也可接管任意角色亲自互动。
> 本文档只覆盖玩法设计与顶层技术考虑，字段级 schema、prompt 模板在实现阶段另行细化。

## 一、模块定位

- 与现有模块的关系：角色卡、世界书复用；呈现方式独立（AVG 式，不用互动故事的纯文本流）；LLM 基建复用 `LlmService`。
- 核心体验：一个小镇在时间里自己运转（NPC 有日程、会相遇、产生故事），玩家决定何时推进、何时介入。

## 二、玩法设计

### 2.1 核心循环

```
查看小镇总览（时间 / 各角色在哪 / 关系网）
        │
        ▼
推进时间 ──→ 产生场景（相遇/事件）──→ 观看场景（AVG 播放）
        │                                   │
        │                                   ├─ 旁观自动演完
        │                                   └─ 若玩家在场：输入行动，NPC 流式反应
        ▼
注入世界事件（可选，「搞事」按钮）
        │
        ▼
跨天 → LLM 生成次日全员行动表 → 循环
```

### 2.2 时间系统

- **手动推进，无后台运行**：关闭 App 小镇时间即暂停，回到 App 继续，不存在离线补算。
- tick = 1 小时（小镇粒度可配置，天级作为备选）。
- 小镇时间显示为「第 N 天 HH:00」。
- 两种推进方式：
  1. **逐小时推进**：一次 +1 小时，细粒度观察；
  2. **推进到下一事件**（主推）：静默套用行动表，直接跳到下一个相遇点/事件点，跳过的时段在日志中只留一行摘要（如「第3天 01:00–06:00，众人在家中安睡」）。
- 跨天瞬间触发次日行动表生成（见 2.3）。

### 2.3 角色与行动表

- NPC 每天有一张**行动表**：若干条 `{时段, 地点, 活动}`。行动表执行是**确定性**的——推进 tick 时由代码直接更新角色位置/活动，**零 LLM 成本**。
- 行动表生成时机：小镇创建时生成第一天；此后每天跨天时**一次调用**批量生成全员次日行动表（不是逐角色调用）。
- 行动表生成会参考：角色 persona、近期经历（记忆）、当前关系、昨日事件、（若存在）已注入的世界事件——保证日程有连续性和因果感。
- 代码侧校验生成的行动表（时段重叠、瞬移、无效地点），违规条目丢弃或修复。
- 行动表对玩家**可见**（上帝视角的一部分，也方便玩家决定去哪「堵人」）。
- **玩家角色没有行动表**，位置由玩家随时指定（去酒馆/去田里），行动表只管 NPC。

### 2.4 场景系统（AVG 呈现）

- **场景（Scene）是一等实体**：`时间 + 地点 + 参与者 + 台词行列表`。
- **相遇检测是纯代码逻辑**：同一时段、同一地点出现 ≥2 个角色 → 生成一个待演场景。地点必须是结构化数据（小镇地点表），不允许自由文本匹配。
- 呈现为 AVG 风格（图像换成文字）：
  - 顶部：地点名 + 小镇时间；
  - 在场角色头像一排（复用角色卡头像）；
  - 旁白框（场景/环境描述）、对话框（说话人名 + 头像 + 台词，打字机流式效果）、动作与心理描写用斜体样式区分；
  - 场景收尾显示「本场景造成的影响」（关系变化、重要记忆等，来自状态提取）。
- 场景生成时机：推进过程中相遇即自动生成（顺序生成，UI 显示进度「正在发生：酒馆的争吵…」），保证日志完整。同一 tick 多个地点同时相遇则依次生成。
- **双视图**：
  - **场景视图**：AVG 播放，支持回看历史场景；
  - **小镇日志**：编年史视图，按时间线罗列每个 tick 摘要与场景条目，是兜底的信息入口。

### 2.5 玩家扮演与互动

- 创建小镇时可选：不扮演（纯上帝视角）/ 扮演某个角色（可中途切换）。
- 玩家玩法：
  - **移动**：随时指定自己所在地点；
  - **旁观**：任何场景都可以上帝视角观看；
  - **互动**：玩家所在地点有 NPC 时，产生「互动场景」——玩家输入台词/行动，场景内 NPC 的反应由 LLM 流式生成（打字机效果），直到玩家离开或对话自然结束。
- 互动场景进行时，**涉及的 NPC 时间线暂停**（场景消耗掉当前时段），结束后 NPC 从行动表下一项继续；其他地点的 NPC 不受影响。
- 互动场景收尾时，同样做状态提取（心情/关系/记忆），玩家说过的话和 NPC 的反应进入相关角色的记忆。
- 玩家的干预天然会打乱 NPC 日程（把人从田里叫去酒馆聊天），这正是生活模拟的乐趣，无需特殊处理——NPC 下一时段自动回归日程。

### 2.6 世界事件（「搞事」按钮）

- **输入：玩家一句话简短描述**（如「镇上来了个旅行商人」「今晚暴雨，河堤危机」）。
- **生成：LLM 展开为完整事件方案**：事件起因经过、发生在哪个时段/地点、涉及哪些角色、对当日行动表的修改建议（如集市日全村去广场）。
- **应用**：事件作为接下来 tick 的输入生效——可能直接产生场景（旅行商人进酒馆）、可能改写行动表（暴雨天没人下田）、事件经过会进入涉事角色的记忆。
- 事件在日志中留痕，与自然相遇一样可回看。
- V1 只有玩家手动注入；V2 再考虑自动随机事件（按概率在跨天时由 LLM 顺带提出次日小事件）。

### 2.7 记忆与关系

每个 NPC 维护三类状态，**关系独立结构化存储，不混在记忆文本里**：

| 状态 | 内容 | 更新时机 |
|---|---|---|
| 近期记忆 | 该角色近 N 个场景的参与记录（滑动窗口，超限丢弃最旧） | 每次场景收尾追加 |
| 重要记忆 | 高影响事件（LLM 生成场景时顺带打重要性分，超阈值才收录） | 场景收尾 / 跨天蒸馏 |
| 关系 | 对其他角色的 `好感度 + 一句话描述`，成对存储 | 场景收尾由工具更新 |

- 构建场景 prompt 时注入：参与者 persona + 各自近期/重要记忆 + 参与者两两关系 + 当前状态——保证角色「记得昨天吵过架」。
- 每个角色另有轻量运行时状态：当前地点、当前活动、心情。
- V2 增加**跨天反思**：每天结束时一次批量调用，把当日经历蒸馏成一句话印象并入重要记忆，控制长期记忆膨胀。

### 2.8 存档与回滚

- 快照 = 小镇整包状态（时间 + 全员状态/行动表 + 关系 + 场景历史指针），存为 JSON。
- 快照点：每天跨天时自动存一份 + 玩家手动存档。
- **回滚用途：重掷**——对某个场景的发展不满意，回到场景前的时间点重新推进（LLM 重新生成）。V1 做线性快照回滚，分支树（参考互动模块检查点的 parentId 结构）留作 V2。

### 2.9 长期可玩性

- 短期靠：相遇组合的随机性 + 世界事件注入 + 关系演化。
- V3 方向：角色长期目标驱动行动表（不只是日程，而是「想达成什么」）；NPC 主动发起对玩家的互动（主动来找你）；地点背景图生成（图像生成模块基建现成，给 AVG 加 CG）。

## 三、顶层技术方案

### 3.1 核心架构原则：Tick 与 LLM 解耦

**tick 是确定性的时钟推进，LLM 是事件驱动的**。逐小时 tick 绝不调 LLM，这是硬规则。LLM 只在四类时机被调用：

| 时机 | 模式 | 频率 |
|---|---|---|
| 跨天行动表生成 | 非流式 + function calling | 1 次/游戏日 |
| 相遇场景生成 | 流式文本（+收尾状态提取工具） | 每次相遇 |
| 玩家互动场景 | 流式文本（+收尾状态提取工具） | 玩家触发 |
| 世界事件展开 | 非流式 + function calling | 玩家触发 |

成本模型：一游戏日约 **3~8 次调用**（1 次行动表 + 数次场景），与角色数量、tick 数量基本无关。对比「每角色每 tick 一调用」（240 次/日）不可行。

### 3.2 LLM 交互的两套模式

- **结构化模式**（行动表、事件展开、状态提取）：走 function calling，参照 `InteractiveStoryService` 的工具循环范式；输出由代码校验。
- **流式模式**（场景对话、NPC 反应）：纯文本流式输出，靠 prompt 约定格式（旁白/角色名: 台词/动作标记），前端逐条解析成 AVG 台词行；场景结束时追加一次轻量工具调用提取状态变更。
- 复用 `LlmService` 双通道；注意 Anthropic 通道目前不支持流式，场景生成依赖流式，需在 UI 提示或做降级（非流式一次性返回）。

### 3.3 数据模型（概念层）

新增独立表集合，一次数据库迁移完成，**不改动**现有聊天/角色卡/互动模块的表：

| 概念实体 | 职责 |
|---|---|
| 小镇 Town | 世界观设定、当前时间（天/小时）、粒度配置、扮演角色 |
| 地点 Location | 结构化地点（名称+描述），相遇检测的基础 |
| 成员 Member | 角色引用 + persona（性格/口吻/目标）+ 是否玩家控制 + 运行时状态（位置/活动/心情）+ 当日行动表 |
| 场景 Scene | 时间 + 地点 + 参与者 + 台词行；**同时是** UI 数据源、记忆原料、日志条目 |
| 关系 Relationship | 成对好感度 + 描述 |
| 快照 Snapshot | 整包状态 JSON |

角色卡的 description 不足以支撑 AVG 式有辨识度的对话，persona 放在 Member 上（小镇私有），不污染角色卡表。

### 3.4 模块分层落点

```
data/local/     Town*Entity + Town*Dao + Migration_15_16
data/repository/ TownRepositoryImpl
domain/model/    Town 领域模型 + 台词行/行动表条目等值对象
domain/service/  TownSimulationService（推进循环、相遇检测、prompt 组装、工具分发，
                 参照 InteractiveStoryService；Scene 相关也收敛在此）
ui/town/         TownListScreen（小镇列表）
                 TownScreen（主界面：总览 + 推进控件 + 日志视图）
                 TownSceneScreen（AVG 场景播放/互动，流式）
                 TownEditScreen（创建/设置：地点管理、成员选择、扮演角色）
ui/navigation/   新增 town_* 路由
PromptBlockType  新增 TOWN_* 提示词块（系统角色/输出要求/成员状态/记忆/关系等），沿用可配置机制
AppContainer     注册新 Repository/Service
```

### 3.5 关键风险与对策

| 风险 | 对策 |
|---|---|
| 上下文随 tick 膨胀 | 日志滑动窗口 + 跨天蒸馏（V2）压缩历史 |
| LLM 输出不合规（日程冲突/瞬移/无效地点） | 代码侧校验，违规丢弃修复，不信任 prompt 约束 |
| 自由文本导致相遇漏检 | 地点强制结构化 ID |
| 同一 tick 多场景生成耗时 | 顺序生成 + 进度提示；V2 可改「摘要先行、点开懒展开」 |
| 后台/进程被杀 | 不做后台模拟，tick 只在前台手动推进（产品特性而非缺陷） |
| 场景质量平庸、日程重复 | 世界事件注入 + 行动表生成时注入昨日事件与关系，鼓励 LLM 制造变化 |
| Anthropic 无流式 | UI 明示或非流式降级 |

## 四、里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| M1 能转起来 | 数据层 + 创建小镇（地点/成员）+ 跨天行动表 + tick 推进 + 相遇检测 + 场景生成（日志视图呈现） | 推进一天，能看到 NPC 按日程移动并产生场景日志 |
| M2 好看 | AVG 场景视图（流式打字机）+ 场景回看 + 快照存档/回滚 | 场景有 AVG 观感，可回滚重掷 |
| M3 好玩 | 玩家扮演角色 + 场景内互动 + 记忆/关系生效 + 「搞事」世界事件 | NPC 记得玩家前一日言行；注入事件改变当日走向 |
| M4（V2+） | 跨天反思蒸馏、场景懒展开、自动小事件、关系网可视化 | — |
| M5（V3） | 目标驱动行动表、NPC 主动互动、地点背景图 | — |
