# MyTavern 功能路线图

> 基于项目现状梳理的待增加/优化功能清单，按优先级排列。

---

## 🔴 高优先级（核心体验缺失）

### 1. 数据备份与恢复（导出/导入）
- **现状**：所有数据完全存储在本地 SQLite，卸载即丢失，用户无法迁移数据。
- **建议**：支持将整个数据库或关键数据导出为 JSON/ZIP，支持本地存储，，并能导入恢复。
- **优先级极高**，这是用户数据安全的基础功能。

### 2. 世界书规则动态匹配
- **现状**：世界书规则只有 `name/description/matchType` 字段，但代码中没有看到根据聊天内容动态匹配并插入相关规则到 Prompt 的逻辑。
- **建议**：实现世界书规则的匹配引擎（如关键词包含匹配），在发送消息时自动将匹配到的规则加入 system prompt。

---

## 🟡 中优先级（用户体验提升）

### 5. 聊天列表信息丰富度
- **现状**：只显示标题和"单聊/群聊"标签。
- **建议**：显示关联角色头像。

### 8. 会话管理增强
- **缺失功能**：
  - 聊天会话重命名
  - 清空整个会话的消息

### 10. 角色详情页
- **现状**：角色列表点击直接进入编辑弹窗，没有独立的详情展示。
- **建议**：增加角色详情页，展示完整描述、关联的聊天记录、统计信息等。


---

## 🟢 低优先级（锦上添花）

### 13. UI/体验细节
- 字体大小调节设置
- 聊天界面双击头像触发回复（已有代码但需确认交互流畅性）
- 滑动返回手势支持
- 屏幕常亮选项（阅读小说时）
- 空状态引导图/动画（替代纯文字提示）

### 14. 技术优化
- **错误处理**：部分流式解析代码存在 `catch (_: Exception)` 静默吞异常的情况，建议至少记录日志。

### 15. 其他功能拓展
- 语音输入（调用系统 Speech-to-Text）
- 图片消息支持（需要视觉模型 API 支持）
- 聊天消息多选批量删除

---

## 📋 推荐优先实施

| 类别 | 最推荐优先做的 3 件事 |
|------|----------------------|
| **数据安全** | 数据导出/导入备份 |
| **核心功能** | 世界书规则动态匹配引擎 |
| **用户体验** | 消息复制 + Markdown 渲染 + 时间戳 |

## 小说助手智能体
结构（ReAct单智能体循环）：
系统级提示词

技能管理
  技能1（名称/内容）...
  技能1（名称/内容）...
工具 
- 读取当前小说元信息
- 读取当前小说第x到y章节大纲
- 读取当前小说第x章内容
- 写入当前小说第x章内容（需要批准）

UI：
入口
 - 创建聊天-智能体
 - 设置系统级提示词 
 - 选择某本小说

